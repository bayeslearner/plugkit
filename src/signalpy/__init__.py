"""SignalPy — A Signal-based reactive component kernel for backend services.

13 decorators. ~2600 lines. Zero dependencies. Async-native.

Usage:
    from signalpy.kernel import (
        Kernel, component, provides, requires, runnable, lifecycle,
        computed, effect, api, prop, subscribe, kind, skill, exportable,
        Signal, batch,
    )
"""
